export const buildChatData = (id, type, text, caption) => {
  return {
    "chatId": "id",
    "type": "TYPE",
    "replyToMessage": "",
    "text": text,
    "caption": "caption for files"
  }
}

// TEXT,SVG,
// JPEG,JPG,
// PNG,PDF